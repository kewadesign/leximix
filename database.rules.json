{
    "rules": {
        "users": {
            "$username": {
                ".read": true,
                "password": {
                    ".write": "!data.exists()"
                },
                "createdAt": {
                    ".write": "!data.exists()"
                },
                "saves": {
                    ".write": true
                }
            }
        }
    }
}